#!/bin/sh
# download fresh data, ingest into sqlite, vectorize new rows
set -e

cd "$(dirname "$0")/.."

echo "[refresh] ingesting from the ships API..."
bundle exec ruby bin/ingest

echo "[refresh] vectorizing..."
bundle exec ruby bin/vectorize

echo "[refresh] done"